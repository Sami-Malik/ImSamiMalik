import React from "react";
import { PlaceholderDiv } from "./AppStyles";

function Placeholder() {
    return (
        <PlaceholderDiv>
            <span>Loading</span>
        </PlaceholderDiv>
    );
}

export default Placeholder;
